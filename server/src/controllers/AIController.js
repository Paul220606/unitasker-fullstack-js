import {model} from "../configs/gemini.js"

class AIController {
    async suggest(req, res) {
        const {title, description} = req.body
        try {
            const prompt = `
            `
        } catch (err) {

        }
    }
}